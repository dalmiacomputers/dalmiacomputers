Dalmia CMS (Option C — Static Export)
- Fill placeholders from Admin UI (next pack) then "Export Static" to /public/pages/*
- All schema auto-injected per page; NAP in footer
- GA4 + review embed slots from Admin